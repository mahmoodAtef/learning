import 'package:flutter/material.dart';

class HonorRoll extends StatelessWidget {
  const HonorRoll({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container (
      child: Center (
        child: Text('honor roll '),
      ),
    ) ;
  }
}
